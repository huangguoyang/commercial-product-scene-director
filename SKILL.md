---
name: commercial-product-scene-director
description: 当用户上传一批参考风格图 + 产品图，要求把产品放进参考图的风格里生成商业大片级场景效果图，复现参考图的光线、质感、构图与审美，且严格保证产品的结构外观与材质不变时使用；也用于「参考图风格复刻+产品换场景」「电商/广告产品场景 KV」「产品大片氛围图」「为同一产品生成系列一致、审美一致的场景提示词」。本技能从多张参考图提炼统一的风格母版，锁定产品身份（结构/材质/颜色不变），沿叙事与审美两个轴衍生多个场景，交付可直接复制的中文生成提示词（默认 16:9），只输出提示词、不自动生图。即使用户只说「把这个产品做出参考图那种大片感」「给我的产品做几张同风格场景图」也应触发。
license: MIT
metadata:
  version: "0.1.0"
---

# 商业产品场景导演

把「参考图的风格」×「产品图的主体」合成商业大片级场景提示词。只输出可复制的提示词，不自动生图。

## 什么时候用

- 用户给了一组参考图（风格来源）+ 一张或多张产品图（主体），要「把产品放进参考图的风格/光线/质感里」。
- 要复现参考图的商业大片调性，同时产品结构、外观、材质不能变。
- 要为同一产品生成系列一致、审美一致的多个场景提示词。
- 用户只说「给我的产品做几张这种风格的场景图 / 大片感 KV / 氛围图」。

## 什么时候不用

- 只有单张参考图、纯图片反推（无产品、无场景合成）→ 用 nuyoah-image-reverse-prompt。
- 没有参考图的通用产品提示词写作。
- 直接改图、抠图、换背景、局部修复。
- 用户只要生图、不要求提炼风格母版或产品锁定。

## 工作流总览

1. **分清输入**：确认哪些是参考图（风格来源）、哪些是产品图（主体，保持不变）。不能把产品图当风格参考，也不能把参考图里的主体当产品。
2. **提炼风格母版**：读 [风格母版](references/style-canon.md)，从多张参考图取「交集」而非「并集」，产出可复用的「风格前缀块」。
3. **锁定产品身份**：读 [产品身份锁](references/product-identity-lock.md)，从产品图提炼结构/材质/颜色等「绝不允许改变」的清单，产出可复用的「产品锁块」。
4. **衍生场景方案**：读 [场景衍生](references/scene-design.md)，沿叙事与审美两个轴设计多个场景，并保证系列一致。
5. **组装提示词**：读 [输出合同](references/output-contract.md)，把风格前缀块 + 产品锁块 + 场景叙事块组装成可直接复制的中文提示词。
6. **覆盖审计**：按输出合同的审计清单逐项核对后再交付。

## 贯穿全程的三条纪律

1. **产品锁优先于一切**。产品结构/材质/颜色是硬约束，任何风格化都不能以牺牲产品可识别性为代价。风格只作用在「场景、光线、色彩氛围、质感调性」上，不作用在产品本体上。
2. **多图取交集，不取并集**。风格母版是「跨参考图共同且可复用」的视觉 DNA；只在某一张图偶发的元素不进母版，除非用户点名参考。
3. **只写有证据的事实**。颜色、光线、材质都要有画面支持；看不清或不确定的省略或标「不确定」，不编造品牌、镜头型号、不可见区域。

## 默认交付

```text
# 风格母版（跨参考图共性）
[媒介/编辑语言、成像签名、色彩系统、光线系统、材质质感、构图语言、氛围调性]
风格前缀块：...

# 产品身份锁（不可改变）
[结构/轮廓、材质、颜色、品牌元素]
产品锁块：...

# 场景方案（系列一致，仅场景变化）
## 场景 1：{叙事一句话}
审美要点：...
产品位置：...
完整提示词：...

## 场景 2 / 3 ...

# 使用建议
画幅默认 16:9；可选：把产品图作为参考图/垫图以更稳保形。
```

默认画幅 16:9。未指定场景数量时给 3 个差异化场景，且三者共享同一风格母版与产品锁块，只有场景/叙事/构图变化。

## 更新本技能

用户明确要求检查或更新本技能时才处理；普通调用不得检查网络版本或运行安装命令。

## 不属于本技能

- 单图反推、无产品、无场景合成。
- 通用产品提示词（无参考图）。
- 直接修图、换背景、抠图、局部修复。
- 自动生图（本技能只产出提示词）。
